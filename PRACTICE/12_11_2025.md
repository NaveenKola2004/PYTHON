## Approach and the logic and problem solving statements in the notes  just go through it

# 🧩 Problem 1: Harshad (Niven) Number Check

- Definition:
- A number is Harshad if it is divisible by the sum of its digits.

- Example:
> 18 → 1 + 8 = 9 → 18 % 9 == 0 ✅ Harshad
> 19 → 1 + 9 = 10 → 19 % 10 != 0 ❌ Not Harshad

#### Hear is the sample code in python 👇👇

```python
def Harshed_Number(n):
    number=n
    sum=0
    while n>0:
        digit=n%10
        sum+=digit
        n//=10
    if sum%number:
        return True
    else:
        return False
n=int(input())

if Harshed_Number(n):
    print("Yes")
else:
    print("No")
```