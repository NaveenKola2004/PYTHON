## Approach and the logic and problem solving statements in the notes  just go through it

# 🧮 Problem 1: Check Strong Number
- This one lets consider 145->1!+4!+5!=145 👍
- The given number and the operation performed result both are equal it is an Stong number
#### Hear is the sample code in python 👇👇

```python
def Fact(digit):
    if digit==0 or digit==1:
        return 1
    return digit*Fact(digit-1)
def Strong(n):
    sum=0
    while(n>0):
        digit=n%10
        sum=sum+Fact(digit)
        n=n//10
    return sum
n=int(input())
Result=Strong(n)
if n==Result:
    print("Stong Number")
else:
    print("Not Strong Number")
```

# 🔢 Problem 2: Check Perfect Number

- This one the perfect number is the lets take the 6 so divisors 1,2,3 -> sum is 6 so the given number and the sum of divisors are equal

#### Hear is the sample code in python 👇👇

```python
def Perfect_Number(n):
    divisors_sum=0
    for i in range(1,n):
        if n%i==0:
            divisors_sum=divisors_sum+i
    return divisors_sum
n=int(input())
Result=Perfect_Number(n)
if n==Result:
    print("Perfect Number")
else:
    print("Not Perfect Number")
```

# 📉 Problem 3: Check Automorphic Number

- In this when the given number like 5 and the 5² = 25 → ends with 5 ✅  25² = 625 → ends with 25 ✅
- pictularlly the number of digits in a number, that square after find the last that number of digits compare

#### Hear is the sample code in python 👇👇

```python
def Count_digits(n):
    count=0
    while(n>0):
        n=n//10
        count+=1
    return count
def Get_Last_digits(n):
    square=n**2
    last_numbers=0
    count=1
    while(count<=Count_digits(n)):
        digit=square%10
        last_numbers=digit+(last_numbers*10)
        count+=1
        square=square//10
    return last_numbers
def Reverse(n):
    number=Get_Last_digits(n)
    rev=0
    while(number>0):
        digit=number%10
        rev=(rev*10)+digit
        number=number//10
    return rev
n=int(input())
auto=Reverse(n)
if a==auto:
    print("✅")
else:
    print("NO")
```

# 💫 Problem 4: Check Neon Number
- This one the given number square ,the resultent square sum is equal to the given that is neon number

- 9² = 81 → 8+1=9 ✅ Neon number

```python
def Neon_Number(n):
    square=n**2
    sum=0
    while(square>0):
        digit=square%10
        sum=sum+digit
        square=square//10
    return sum
n=int(input())
if n==Neon_Number(n):
    print("Yes")
else:
    print("No")
```

# 🔁 Problem 5: Sum of Factors

- This one Input: 12 → Divisors = 1, 2, 3, 4, 6, 12 → Output: 28

```python
def Sum_Factors(n):
    sum=0
    for i in range(1,n+1):
        if n%i==0:
            sum=sum+i
    return sum
n=int(input())
print(Sum_Factors(n))
```