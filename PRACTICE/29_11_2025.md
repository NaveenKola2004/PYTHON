## Approach and the logic and problem solving statements in the notes  just go through it

# problem 1 Happy number 

```python

🎯 Example: Check if 19 is a Happy Number

Let’s do the ritual:

Step 1:

1² + 9²
= 1 + 81
= 82

Step 2:

8² + 2²
= 64 + 4
= 68

Step 3:

6² + 8²
= 36 + 64
= 100

Step 4:

1² + 0² + 0²
= 1 + 0 + 0
= 1

🔥 Since we reached 1, 19 is a Happy Number.
```

### Hear is the sample code in python 👇👇

```python

def Sum(n):
    sum=0
    while n>0:
        digit=n%10
        sum+=digit**2
        n//=10
    return sum
def Solution(num):
    seen=set()
    store=num
    while store>1:
        if store in seen:
            return False
        seen.add(store)
        store=Sum(store)
    return True
n=int(input())
print(Solution(n))

```

# Problem 2 Digital Root 

```python
🎯 Example 1: Digital root of 9875

Step 1:
9 + 8 + 7 + 5 = 29

Step 2:
2 + 9 = 11

Step 3:
1 + 1 = 2

👉 Digital root = 2
```

### Hear is the sample code in python 👇👇

```python
def Sum(n):
    count=0
    sum=0
    while n>0:
        digit=n%10
        sum+=digit
        count+=1
        n//=10
    return count,sum
def Solution(n):
    count,sum=Sum(n)
    while count>1:
        count,sum=Sum(sum)
    return sum
n=int(input())
print(Solution(n))
```

# problem 3 Disarium Number

```python 
Example:

135
1¹ + 3² + 5³
= 1 + 9 + 125
= 135 ✔ Disarium
```

### Hear is the sample code in python 👇👇

```python
def Count(n):
    count=0
    while n>0:
        count+=1
        n//=10
    return count
def Solution(n):
    sum=0
    dup=n
    i=Count(n)
    while n>0:
        digit=n%10
        sum+=digit**i
        i-=1
        n//=10
    if sum==dup:
        print("YES")
    else:
        print("NO")
n=int(input())
Solution(n)
```

# problem 4 Evil Number 

```python
A number is Evil if the number of 1’s in its binary representation is even.

Example:

9 → binary = 1001 → two 1's → ✔ Evil

11 → binary = 1011 → three 1's → ❌ Not Evil
```

### Hear is the sample code in python 👇👇

```python
def Solution(n):
    if n==1:
        return "0"
    binary=""
    while n>0:
        rem=n%2
        binary=str(rem)+binary
        n//=2
    count=binary.count("1")
    if count %2==0:
        return "YES"
    else:
        return "NO"
n=int(input())
print(Solution(n))
```

# problem 5 Duck Number

```python
Examples:

402 → ✔ Duck

707 → ✔ Duck

123 → ❌ Not Duck

012 → ❌ Invalid (leading zero not allowed)
```

### Hear is the sample code in python 👇👇

```python

```