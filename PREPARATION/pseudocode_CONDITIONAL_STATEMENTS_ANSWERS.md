```
-- ===================================================
--   ANSWERS/OUTPUTS FOR PLACEMENT PSEUDOCODES
-- ===================================================

-- 1. FizzBuzz
-- Output: Prints numbers 1 to 100, with substitutions.
-- 1, 2, Fizz, 4, Buzz, Fizz, 7, 8, Fizz, Buzz, 11, Fizz, 13, 14, FizzBuzz, 16, ...

-- 2. Prime Number Check
-- Example Call: isPrime(7) -> RETURNS: true
-- Example Call: isPrime(10) -> RETURNS: false

-- 3. Factorial (Using Recursion)
-- Example Call: Factorial(5) -> RETURNS: 120

-- 4. Fibonacci Sequence (Using Iteration)
-- Example Call: Fibonacci(6) -> RETURNS: 8

-- 5. Find Maximum Element in an Array
-- Example Call: FindMax([3, 29, 1, 15, 8]) -> RETURNS: 29

-- 6. Reverse an Array
-- Example: An array [10, 20, 30, 40] becomes [40, 30, 20, 10]

-- 7. Check for Palindrome String
-- Example Call: isPalindrome("madam") -> RETURNS: true
-- Example Call: isPalindrome("hello") -> RETURNS: false

-- 8. Two Sum Problem
-- Example Call: TwoSum([2, 7, 11, 15], 9) -> RETURNS: [0, 1] (the indices of 2 and 7)

-- 9. Linear Search
-- Example Call: LinearSearch(["a", "b", "c"], "b") -> RETURNS: 1

-- 10. Binary Search
-- Example Call: BinarySearch([10, 20, 30, 40, 50], 40) -> RETURNS: 3

-- 11. Bubble Sort
-- Example: An array [5, 1, 4, 2, 8] becomes [1, 2, 4, 5, 8]

-- 12. Selection Sort
-- Example: An array [64, 25, 12, 22, 11] becomes [11, 12, 22, 25, 64]

-- 13. Linked List: Traversal
-- Example: For a list 10 -> 20 -> 30
-- Output:
-- 10
-- 20
-- 30

-- 14. Linked List: Insert at End
-- Example: A list 10 -> 20, after inserting 30, becomes 10 -> 20 -> 30

-- 15. Stack: Push and Pop
-- Output:
-- 20
-- 10

-- 16. Queue: Enqueue and Dequeue
-- Output:
-- "A"
-- "B"

-- 17. Stack Application: Balanced Parentheses
-- Example Call: isBalanced("{[()]}") -> RETURNS: true
-- Example Call: isBalanced("{[(])}") -> RETURNS: false

-- 18. Hash Table (Dictionary): Frequency Count
-- Example Call: CountFrequencies(["a", "b", "a", "c", "a"])
-- RETURNS: A dictionary/map like {"a": 3, "b": 1, "c": 1}

-- 19. Binary Search Tree (BST): Insertion
-- Example: An empty tree, after inserting 10, 5, 15 would have 10 as root,
-- 5 as the left child of 10, and 15 as the right child of 10.

-- 20. Binary Tree: In-order Traversal
-- Example: For a BST containing nodes 10, 5, 15
-- Output:
-- 5
-- 10
-- 15

-- 21. Graph: Breadth-First Search (BFS)
-- Example: For a graph A -> {B, C}, B -> {D}, start at A
-- Output (one possible order):
-- A
-- B
-- C
-- D

-- 22. Graph: Depth-First Search (DFS)
-- Example: For a graph A -> {B, C}, B -> {D}, start at A
-- Output (one possible order):
-- A
-- C
-- B
-- D
```