# Creating the class and the object 

### Hear is the simple thing to create an object and the class

```python 
# Creating the class
class Car:
    def Satrt(self):
        print("Starting")

# Creating the object
my_car=Car()
my_car.Satrt()
```

#### mini Project 
- Today created the Accounts creation and the fetch the details based on account number
```python
class Account:
    def __init__(self,Account_number,Name,Location,Account_type,Initial_deposit):
        self.Account_number=Account_number
        self.Name=Name
        self.Location=Location
        self.Account_type=Account_type
        self.Initial_deposit=Initial_deposit
    def Show(self):
        print(f"\nAccount_Number : {self.Account_number}\nName : {self.Name} \nLocation : {self.Location} \nAccount Type : {self.Account_type} \nBalance : {self.Initial_deposit}")
        print("-------------------\n")

Accounts=[]
account_number=1
i=1
while i>0:
    print("1.Create account \n2.Retrieve details")
    user_input=int(input("Enter the number what you want to do : "))
    match(user_input):
        case 1 : 
            print("You are choose create an new account")
            name=input("Enter your name : ")
            Location=input("Enter your location : ")
            Account_type=int(input("Choose Account type 1. Savings Account 2. Current Account : "))
            if Account_type==1:
                Initial_deposit=int(input(f"you choose Savings account so you have to  deposit min 500 Rupes : "))
                A=Account(account_number,name,Location,Account_type,Initial_deposit)
                Accounts.append(A)
                account_number+=1
            else:
                Initial_deposit=int(input(f"Enter the deposit Ammount : "))
                A=Account(account_number,name,Location,Account_type,Initial_deposit)
                Accounts.append(A)
                account_number+=1
        case 2:
            print("You want to see the account details ")
            user_Enter_Number=int(input("Enter the account number : "))
            found=False
            for acc in Accounts:
                if acc.Account_number==user_Enter_Number:
                    acc.Show()
                    found=True
                    break
            if not found:
                print("Account not found ")
        case 0:
            i=0
```